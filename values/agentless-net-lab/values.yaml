# Agentless Networking E2E Lab Values
#
# Based on caas-ci values. Key difference:
# - NETWORK_STEPS_COLLECTION: agentless_net.steps (direct switch + net-node)
# - AGENTLESS_NET_* vars for the containerlab lab topology

# --- Prerequisites ---
certManager:
  enabled: true
trustManager:
  enabled: true
caIssuer:
  enabled: true
keycloak:
  enabled: true
aapOperator:
  enabled: true
lvms:
  enabled: true
metallb:
  enabled: true
cnv:
  enabled: false
mce:
  enabled: true

# --- Operator ---
operator:
  image:
    repository: ghcr.io/osac-project/osac-operator
    tag: sha-cae6dbe
    pullPolicy: Always
  provisioning:
    provider: "aap"
  aap:
    insecureSkipVerify: "true"
    tokenSecret:
      name: osac-aap-api-token
      key: token
  fulfillment:
    serverAddress: "fulfillment-internal-api:8001"
    tokenFile: "/var/run/secrets/kubernetes.io/serviceaccount/token"
  controllers:
    clusterOrder: true
    computeInstance: false
    tenant: true
    networking: true
    storage: true
  hubAccess:
    enabled: true
  tenants:
  - name: shared
  - name: osac-e2e-ci

# --- Fulfillment Service ---
service:
  variant: openshift
  externalHostname: ""
  internalHostname: ""
  images:
    service: ghcr.io/osac-project/fulfillment-service:sha-1ce8922
    envoy: ghcr.io/osac-project/envoy:v1.33.0
  certs:
    issuerRef:
      kind: ClusterIssuer
      name: default-ca
    caBundle:
      configMap: ca-bundle
  auth:
    issuerUrl: https://keycloak.keycloak.svc.cluster.local/realms/osac
    emergencyServiceAccounts:
    - admin
    - osac-operator
    - osac-operator-controller-manager
    - template-publisher
    controllerCredentials:
    - secret:
        name: fulfillment-controller-credentials
        items:
        - key: client-id
          param: client-id
        - key: client-secret
          param: client-secret
  idp:
    provider: keycloak
    url: https://keycloak.keycloak.svc.cluster.local
    credentials:
    - secret:
        name: fulfillment-controller-credentials
        items:
        - key: client-id
          param: client-id
        - key: client-secret
          param: client-secret
  database:
    connection:
    - secret:
        name: fulfillment-db
        items:
        - key: url
          param: url
    - secret:
        name: postgres-client-cert-service
        items:
        - key: tls.crt
          param: sslcert
        - key: tls.key
          param: sslkey
        - key: ca.crt
          param: sslrootcert
  log:
    level: debug

# --- AAP ---
aap:
  aap:
    instance:
      enabled: true
      name: "osac-aap"
  bootstrap:
    enabled: true
    image: ghcr.io/osac-project/osac-aap:sha-7ef486d
    cliImage: quay.io/openshift/origin-cli:4.20.0
    backoffLimit: 15
  configAsCode:
    manifestSecret: "config-as-code-manifest-ig"
    secret: "config-as-code-ig"
    eeImage: "ghcr.io/osac-project/osac-aap:sha-7ef486d"
    projectGitUri: "https://github.com/ybettan/osac-aap"
    projectGitBranch: "agentless-net"
  instanceGroups:
    clusterFulfillment:
      enabled: true
      config:
        NETWORK_STEPS_COLLECTION: "agentless_net.steps"
        HOSTED_CLUSTER_CONTROLLER_AVAILABILITY_POLICY: "SingleReplica"
        HOSTED_CLUSTER_INFRASTRUCTURE_AVAILABILITY_POLICY: "SingleReplica"
        AGENTLESS_NET_VLAN_POOL_START: "100"
        AGENTLESS_NET_VLAN_POOL_END: "199"
        AGENTLESS_NET_TRUNK_INTERFACE: "eth1"
        AGENTLESS_NET_EXTERNAL_INTERFACE: "eth0"
        AGENTLESS_NET_EXTERNAL_GATEWAY: "172.20.20.1"
        AGENTLESS_NET_SUBNET_CIDR: "10.100.0.0/24"
        AGENTLESS_NET_PUBLIC_IP_POOL_START: "192.168.100.10"
        AGENTLESS_NET_PUBLIC_IP_POOL_END: "192.168.100.50"
    networkFulfillment:
      enabled: true

# --- Validation ---
validation:
  enabled: true

# --- Bare Metal Fulfillment Operator ---
bmf:
  enabled: false
  image:
    repository: ghcr.io/osac-project/bare-metal-fulfillment-operator
    tag: sha-d28b3ca
    pullPolicy: Always

# --- CI/dev only ---
hubAccess:
  enabled: true

bundledPostgres:
  enabled: true
  database:
    name: service
